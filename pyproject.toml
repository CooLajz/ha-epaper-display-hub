[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ha-epaper-display-hub"
version = "0.3.0"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
test = [
  "mypy>=1.15",
  "pytest>=8.3",
  "pytest-asyncio>=0.25",
  "pytest-cov>=6.0",
  "pytest-homeassistant-custom-component>=0.13.230",
  "ruff>=0.11"
]

[tool.setuptools.packages.find]
include = ["custom_components*"]
namespaces = true

[tool.setuptools.package-data]
"custom_components.coolajz_epaper_display_hub" = [
  "brand/*.png",
  "brand/*.svg",
  "manifest.json",
  "translations/*.json"
]

[tool.pytest.ini_options]
addopts = "-q --strict-markers"
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
line-length = 88

[tool.ruff.lint]
select = ["ASYNC", "B", "C4", "E", "F", "I", "N", "PERF", "RUF", "UP", "W"]

[tool.mypy]
python_version = "3.13"
strict = true
warn_unreachable = true
